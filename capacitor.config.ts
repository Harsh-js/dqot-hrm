import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'notus-nextjs',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
